services:
  - type: web
    name: sportiq-betting-app
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: SPORTS_API_KEY
        sync: false
